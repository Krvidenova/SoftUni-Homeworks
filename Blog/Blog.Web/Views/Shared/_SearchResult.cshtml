@model SearchResultViewModel

<h3 class="text-center pb-50" style="color: #4cd3e3">@Model.Posts.Count() results found for "@Model.SearchTerm"</h3>

@if (!Model.Posts.Any())
{
    <div class="pt-150"><div class="pt-150"></div></div>
}
else
{
    @Html.DisplayFor(model => model.Posts)
}

